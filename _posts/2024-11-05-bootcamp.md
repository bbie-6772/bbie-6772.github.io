---
layout: post
title: 본캠프 9일차 - [프로그래밍 심화 주차]
subtitle: 게임서버 개발 부트캠프
author: bbie
categories: bootcamp
banner:
  image: https://video.spartacodingclub.kr/classroom/thumb/hanghae_new_node_1st_classroom_thumbnail.png
  opacity: 0.618
  background: "#000"
  height: "50vh"
  min_height: "20vh"
tags: [javascript, node.js, codetest]
---

## 한줄평

- 화이팅!

## 오늘의 삽질

## 개선점 분석

## 지식창고

### [Node.js 입문] 강의


### 알고리즘 코드 카타

오늘의 과제 - 최대 공약수와 공배수

```jsx
function solution(n, m) {
    //최대 공약수 = 둘을 동시에 나눴을 떄 가장 큰 수
    let prime_result = 0;
    //배수 계산 시 최댓값 선정 = 두 값의 곱
    let mul_n = [];
    let mul_m = [];
    // 공배수
    let equal_multiple = [];
    // 답 제출
    let answer = [];
    // 더 작은 수 구하기
    let min = Math.min(n,m);
    
    // 작은 수를 기준으로 둘을 나눴을 때 가장 높은 값 찾기
    for (let i = 1;i <= min;i++) {
        if (n % i === 0  && m % i === 0) {prime_result = i};
    }

    // 최대 공약수 제출
    answer.push(prime_result);

    //두 수를 곱한 수 이하에서 배수 찾기
    for (let j = 1;j <= m;j++){
        mul_n.push(n * j)
    }
    for (let j2 = 1;j2 <= n;j2++){
        mul_m.push(m * j2)
    }
    
    //공배수 배열들의 교집합 구하기
    equal_multiple = mul_n.filter(x => mul_m.includes(x));
    
    // 구조분해할당으로 문자를 숫자로 인식시켜 최대 공배수 제출
    answer.push(Math.min(...equal_multiple));
    
    return answer;
}
```

---
