---
layout: post
title: 본캠프 31일차 - [프로그래밍 심화 주차]
subtitle: 게임서버 개발 부트캠프
author: bbie
categories: bootcamp
banner:
  image: https://cdn.oneesports.gg/cdn-data/2024/07/TeamfightTactics_Set12_MagicandMayhem_KeyVisual.jpg
  opacity: 0.618
  background: "#000"
  height: "50vh"
  min_height: "20vh"
tags: [codetest, petit-tft-project ]
---

## 오늘의 주요내용

### 스탠다드 반

- 현재 블로그를 작성할 때 보는 사람의 입장이 아닌, 그냥 되는대로 작성하는 것 같다는 지적을 받게 되었다.

> 그래서 작성 방식에 대한 개선점을 찾아보았다.  
  
#### 디버깅 접근성 개선

- 블로그에 글을 작성할 때, VScode의 마크다운 화면만 확인하기 때문에 블로그의 실제 페이지를 확인해가며 수정하기 어렵다!

> Github Page가 아닌 로컬에서 실시간 빌드 하는 방법을 찾아 화면을 확인하며 수정해보자!

1. 로컬에서 Github Pages에서 쓰는 **Jekyll(블로그 생성 프로그램)**을 사용하기 위해 설치를 진행한다!

2. 설치를 진행하기 위해 **Ruby(동적 객체 지향 프로그래밍 언어)를 먼저 설치**해야 한다..  
(Jekyll은 Ruby로 작성되어 있기 때문이다!)

3. Ruby 설치 후 명령 프롬프트를 실행하여 아래의 명령어를 입력 해주면 Jekyll 설치가 마무리 된다!

    ```bash
    gem install jekyll bundler
    ```

4. 실행시킬 프로젝트(폴더)의 오류를 줄이기 위해 Bundler를 설치해준다!(대부분의 최신버전 Ruby 설치 시 포함되어있음)

5. Git bash에서 실행할 Jekyll 폴더로 들어가 아래의 명령어들을 입력해준다!

    ```bash
    # 폴더에 번들 설치
    bundle install
    # 번들을 이용해 jekyll 실행
    bundle exec jekyll serve
    ```

6. 이후 localhost:4000 에 들어가면 정상적으로 실행이 된다!

    ![로컬블로그](https://github.com/user-attachments/assets/312c02c0-22e7-4296-8ae3-fdd4fe24a548)

7. 마지막으로 명령어 입력없이 쉽게 jekyll을 실행 해주기 위해 **jekyll run(VScode 확장 프로그램)** 을 설치 해준다!

    ![jekyll-run](https://github.com/user-attachments/assets/e4ecb3fc-b5af-4d2c-b189-49234270bacd)

    - 아래의 버튼을 눌러 쉽게 실행하고 재시작/멈춤 을 제어할 수 있다!

#### 시간 배분 개선

- 과제 및 공부를 하고난 후, 정리를 하는 시간이 부족하다고 생각을 많이 했다!

1. 과제 및 공부 진행중 중요하다 생각되는 부분/단어를 적어두자

2. 일에 우선순위를 정하여 블로그 정리 하는 시간을 소홀히 하지 않도록 노력해야겠다.

## 쁘띠 TFT 프로젝트

오늘은 프로젝트를 마치고 발표 및 피드백을 받게되는 날이였고, 피드백에 관한 생각들을 정리해보았다.

### 피드백

#### 트랜잭션의 최적화

- 트랜잭션 내부에 await을 사용하는 경우 동기적 작업이 직렬로 되어, 트랜잭션 작업속도가 느려질 수 있다.

    => 트랜잭션의 경우 설정한 격리수준에 따라 사용하는 DB로의 접근을 막기 때문에 성능저하가 일어날 수 있다.

- **해결책**

1. 트랜잭션 외부에서 await을 통해 비동기 처리를 미리 해두어 트랜잭션 내부에선 최소한의 작업이 되도록 만들기

    ```jsx
    const data = await fetchData(); // 트랜잭션 외부에서 비동기 작업 수행

    await db.transaction(async (trx) => {
        await trx('table').insert(data); // 트랜잭션 내부 작업 최소화
    });
    ```

2. Promise.all을 이용해 병렬적 실행하기

    ```jsx
    // 트랜잭션 내부
    await Promise.all([
        trx('table1').insert(data1),
        trx('table2').update(data2),
    ]);
    ```

- **실험**

Promise.all()을 이용하면 동시호출과 실행순서가 지켜지면서, 병렬적으로 시스템을 실행하기 때문에 시간이 더욱 적게 사용된다!

- for문과 await을 이용한 실행

```jsx
async function test() {
    const list = [3000, 2000, 3000, 4000, 1000, 1000, 2000, 3000];
    //list의 내부 시간만큼 타이머 실행 이후 값을 반환
    const testFunction = function (num) {
        return new Promise((resolve) => setTimeout(() => resolve(`${num}`), num));
    }
    //시간 측정
    console.time()
    // for 과 await 사용
    for (let i = 0; i < list.length; i++) {
        const result = await testFunction(list[i]);
        console.log(result);
    }
    //측정 끝
    console.timeEnd()
}
```

![await실험1](https://github.com/user-attachments/assets/cd4ed641-2f79-42e9-8715-3e25300c38de)

=> 예상대로 순차적으로 나왔으며, 실행된 시간은 3 + 2 + 3 + 4 + 1 + 1 + 2 + 3 = 17 ≓ 19 초가 나왔다.

- Promise.all()과 map()을 이용한 실행

```jsx
async function test() {
    const list = [3000, 2000, 3000, 4000, 1000, 1000, 2000, 3000];
    //list의 내부 시간만큼 타이머 실행 이후 값을 반환
    const testFunction = function (num) {
        return new Promise((resolve) => setTimeout(() => resolve(`${num}`), num));
    }
    //시간측정
    console.time()
    // Promise.all 과 map() 사용
    Promise.all(list.map((e) => testFunction(e)))
        .then((results) => {
            console.log(results)
            // 측정 끝
            console.timeEnd() 
         })
}
```

![await실험2](https://github.com/user-attachments/assets/173d3020-8643-483e-898d-61e73a1e13b1)

=> 이번엔 순서대로 나왔지만, 최대값인 4초를 기준으로 프로세스가 마무리 되었다!

#### RESTful API 형식 미비

RESTful한 API 형식이 제대로 반영되지 않은 것 같다.

=> 어떤 형식의 어느 부분이 반영되지 않은 것인지 후에 받을 피드백 부분에서 확인해 봐야겠다.

## 지식창고

### Reference

[스파르타코딩클럽](https://spartacodingclub.kr/)  
[Github Page 로컬테스트](https://docs.github.com/ko/enterprise-cloud@latest/pages/setting-up-a-github-pages-site-with-jekyll/testing-your-github-pages-site-locally-with-jekyll)  
[Jekyll 공식사이트](https://jekyllrb.com/docs/installation/)  
[RubyInstaller(Window)](https://rubyinstaller.org/)

---
